import { ChainId } from '@liquality/cryptoassets';
import { Network } from '../store/types';
import { ChainifyNetwork } from '../types';
export declare const defaultChainSettings: Record<Network, Record<ChainId, ChainifyNetwork>>;
