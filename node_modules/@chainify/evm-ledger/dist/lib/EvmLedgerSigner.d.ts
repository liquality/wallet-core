import { Address } from '@chainify/types';
import { Provider, TransactionRequest } from '@ethersproject/abstract-provider';
import { Signer } from '@ethersproject/abstract-signer';
import { Bytes } from '@ethersproject/bytes';
import { GetAppType } from './types';
export declare class EvmLedgerSigner extends Signer {
    provider: Provider;
    private addressCache;
    private readonly getApp;
    private readonly derivationPath;
    constructor(getApp: GetAppType, derivationPath?: string, provider?: Provider, addressCache?: Address);
    getAddress(): Promise<string>;
    signMessage(message: Bytes | string): Promise<string>;
    signTransaction(transaction: TransactionRequest): Promise<string>;
    connect(provider: Provider): EvmLedgerSigner;
    private _retry;
}
