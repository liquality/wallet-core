import Transport from '@ledgerhq/hw-transport';
import { TransportCreator } from './types';
export declare class WebHidTransportCreator implements TransportCreator {
    private _transport;
    private _onDisconnectCallbacks;
    create(onDisconnect?: () => void): Promise<Transport>;
}
