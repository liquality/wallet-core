import { Chain } from '@chainify/client';
import { AddressType, Asset, BigNumber, Block, FeeDetails, Transaction } from '@chainify/types';
import { providers } from 'near-api-js';
import { BlockResult, NearNetwork, NearTxLog } from '../types';
export declare class NearChainProvider extends Chain<providers.JsonRpcProvider> {
    constructor(network: NearNetwork, provider?: providers.JsonRpcProvider);
    getBlockByHash(blockHash: string, includeTx?: boolean): Promise<Block<BlockResult, Transaction>>;
    getBlockByNumber(blockNumber?: number, includeTx?: boolean): Promise<Block<BlockResult, Transaction>>;
    getBlockHeight(): Promise<number>;
    getTransactionByHash(txHash: string): Promise<Transaction<NearTxLog>>;
    /**
     * @param _assets - it's ignored as Chanify does not support Near tokens yet
     */
    getBalance(addresses: AddressType[], _assets: Asset[]): Promise<BigNumber[]>;
    getFees(): Promise<FeeDetails>;
    sendRawTransaction(rawTransaction: string): Promise<string>;
    sendRpcRequest(method: string, params: any[]): Promise<any>;
    _getBlockById(blockId: number | string, includeTx: boolean): Promise<Block<BlockResult, any>>;
    private getAccount;
}
