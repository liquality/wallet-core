import { HttpRequest } from "../utils";
export declare class ABIService {
    httpRequest: HttpRequest;
    constructor(baseUrl: string);
    getABI(network: string, version: string, bridgeType: string, contractName: string): Promise<any>;
    getAddress(network: string, version: string): Promise<unknown>;
}
