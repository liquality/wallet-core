import { BaseContractMethod } from "../abstracts";
import { Logger } from "../utils";
export declare abstract class BaseContract {
    address: string;
    logger: Logger;
    constructor(address: string, logger: Logger);
    abstract method(methodName: string, ...args: any[]): BaseContractMethod;
}
