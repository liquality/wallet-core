export declare class ABIManager {
    networkName: string;
    version: string;
    constructor(networkName: string, version: string);
    init(): Promise<void>;
    getConfig(path: string): any;
    getABI(contractName: string, bridgeType?: string): Promise<any>;
    setABI(contractName: string, bridgeType: string, abi: any): void;
}
